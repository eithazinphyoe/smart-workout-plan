

<!doctype html>
<html lang="en" data-bs-theme="auto">
  
  <head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <meta name="generator" content="Hugo 0.115.4">
    <title>Workout update</title>
    <% include ../../partialA/head %>

  </head>
  <body>
<% include ../../partialA/header %>

<div class="container-fluid">
  <div class="row">
 <% include ../../partialA/sidebar %>

    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
        <h1>Workout Update</h1>
        <div class="row">
          <div class="col-sm-12 col-md-12 col-lg-3"></div>
          <div class="col-sm-12 col-md-12 col-lg-6 border border-1 p-3 rounded border-primary mt-5"
          >
          <div class="form-signin w-100 m-auto">
            <h1 class="h3 mb-3 fw-normal">Workout Update</h1>
            <form action="/admin/workoutadd" method="post" enctype="multipart/form-data">
              <div class="form-floating my-2">
                <input type="hidden" name="id" value="<%= workout._id %>"/>
                <input type="text" class="form-control" id="title" placeholder="title" value="<%= workout.title %>" name="title">
                <label for="title">title</label>
              </div>
              <div class="form-floating my-2">
                <input type="text" class="form-control" id="type" placeholder="type" value="<%= workout.type %>" name="type">
                <label for="type">Type</label>
              </div>
              <div class="form-floating my-2">
                <select name="category" id="category" class="form-select">
                  <option value="gain" <%= workout.type %>>Weight Gain</option>
                  <option value="lose" <%= workout.type %>>Weight Lose</option>
                  <option value="fitness">Fitness</option>
              </select>
              <label for="category">Category</label>
              </div>
              <div class="input-group my-2">
                <input type="file" class="form-control" id="cover" placeholder="cover" name="cover" accept="image/*">
                <label for="cover" class="input-group-text">Cover</label>
              </div>
              <div class="form-floating my-2">
                <textarea class="form-control" id="description" placeholder="description"  name="description" style="height: 100px;"><%= workout.description %></textarea>
                <label for="description">Description</label>
              </div>
              <div class="d-grid">
                <input type="submit" class="btn btn-primary" value="Submit"/>
              </div>
            </form>
          </div>
        </div>
          <div class="col-sm-12 col-md-12 col-lg-3"></div>
        </div>
        <form action="/admin/workoutupdate" method="post" enctype="multipart/form-data">
            <input type="hidden" name="id" value="<%= workout._id %>"/>
        <label for="title" >Title</label>
        <input type="text" name="title" id="title" value="<%= workout.title %>"/><br><br>
        <label for="type">Type</label>
        <input type="text" name="type" id="type" value="<%= workout.type %>"/><br><br>
        <label for="category">Category</label>
        <select name="category" id="category">
            <option value="gain" <%= workout.category =='gain'? 'selected':'' %>>Weight Gain</option>
            <option value="lose"  <%= workout.category =='lose'? 'selected':'' %>>Weight Lose</option>
            <option value="fitness"  <%= workout.category =='fitness'? 'selected':'' %>>Fitness</option>
        </select><br><br>
        <label for="cover">Cover</label>
        <input type="file" name="cover" id="cover" accept="image/*"/><br><br>
        <label for="description" >Description</label>
        <textarea name="description" id="description" cols="30" row="10"><%= workout.description %></textarea><br><br>
        <input type="submit" value="Submit"/>
        </form> 

      
     
    </main>
  </div>
</div>


    </body>
</html>